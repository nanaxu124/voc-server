async function load(){setState('正在调用 voc_npi_intent_details…');try{const j=await getJSON(`/api/mcp/intent_data?${params()}`),d=j.data||{},s=d.summary||{};$('summary').innerHTML=Object.entries(s).slice(0,4).map(([k,v])=>`<div class="card"><div class="kpi-label">${esc(k)}</div><div class="kpi-value">${fmt(v)}</div></div>`).join('');$('rows').innerHTML=table(d.rows||[],150);$('raw').innerHTML=raw(j.raw_text);setState(`已更新 · ${j.source}`)}catch(e){setState(e.message,true)}}bindReload(load);load();
